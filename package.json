{
  "name": "inventory-reorder-prediction-system",
  "version": "1.0.0",
  "description": "Professional Inventory Reorder Prediction System with Variance Analysis Dashboard",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["inventory", "manufacturing", "variance-analysis", "prediction", "dashboard"],
  "author": "Inventory System Pro",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
